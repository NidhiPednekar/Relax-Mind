specVersion: "v1beta"
serviceId: "relaxmind"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "relaxmind-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
